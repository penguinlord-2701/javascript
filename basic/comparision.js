console.log(null > 0);
console.log(null >= 0) // null is converted to number and then does comparison

// === strict check  => check data type also 
 console.log("2"=== 2) // returns false value 

 // use clean code because that is given more priority 

 //  > < >= <=  && >! !=  this are normal comparision

